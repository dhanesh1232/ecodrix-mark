/** @type {import('next').NextConfig} */
const nextConfig = {
  darkMode: "class",
  images: {
    domains: [
      "randomuser.me",
      "images.unsplash.com",
      "images.pexels.com",
      "res.cloudinary.com",
    ],
  },
};

export default nextConfig;
